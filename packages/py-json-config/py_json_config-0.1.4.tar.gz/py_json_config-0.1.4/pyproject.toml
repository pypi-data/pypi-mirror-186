[tool.poetry]
name = "py-json-config"
version = "0.1.4"
description = "JSONConfig was created to use .json files as configs"
authors = ["Enveloss"]
readme = "README.md"
packages = [{include = "py_json_config"}]

[tool.poetry.dependencies]
python = ">3.5"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
