[tox]
envlist = py{310}
isolated_build = True

[testenv]
extras = test, cpu
passenv =
    CI
    GITHUB_*
    TOXENV
commands =
    pytest tests/ --cov --cov-report term --cov-report xml --cov-config={toxinidir}/pyproject.toml
