[tool.poetry]
name = "flowgl"
version = "0.1.3"
description = "Flow Immersive python client for datasets API"
authors = ["Flow Immersive <info@flow.gl>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"
requests-toolbelt = "^0.10.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
