[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.black]
target-version = ["py37", "py38", "py39", "py310", "py311"]

[tool.isort]
profile = "black"
verbose = true

[tool.mypy]
strict = true

[[tool.mypy.overrides]]
module = "soco.*"
ignore_missing_imports = true

[tool.poetry]
name = "pisco"
version = "0.4.7"   # Version will be set by CI workflow based on git tag.
description = "Keyboard only controller for Sonos speakers"
authors = ["Christoph Gietl <christophgietl@users.noreply.github.com>"]
license = "MIT"
readme = "README.md"
repository = "https://github.com/christophgietl/pisco"

[tool.poetry.dependencies]
python = "^3.7"
click = "^8.1"
Pillow = "^9.4"
python-json-logger = "^2.0"
requests = "^2.28"
soco = "^0.29.0"
xdg = "^5.1"

[tool.poetry.dev-dependencies]
mypy = ">=0.991, <0.1000"
pre-commit = "^2.21"
pytest = "^7.2"
pytest-cov = "^4.0"
types-Pillow = "^9.4"
types-requests = "^2.28"

[tool.poetry.scripts]
pisco = "pisco:main"
