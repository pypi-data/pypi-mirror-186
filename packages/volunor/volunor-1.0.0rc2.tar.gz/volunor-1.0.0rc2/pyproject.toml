[tool.poetry]
name = "volunor"
version = "1.0.0rc2"
description = "Command line tools, with ease"
authors = ["Thomas Mahe <contact@tmahe.dev>"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.7"

[tool.poetry.group.dev.dependencies]
coverage = "^7.0.5"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
