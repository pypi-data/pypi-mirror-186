[package]
name = "_fol_embedding"
version = "0.1.4"
edition = "2021"

[package.metadata.maturin]
name = "fol_embedding._fol_embedding"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html
[lib]
name = "_fol_embedding"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.17.1", features = ["extension-module"] }
tch = "0.10.1"