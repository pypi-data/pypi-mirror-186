[tool.poetry]
name = "prometheus-distributed-client"
version = "1.2.2"
description = ""
authors = ["François Schmidts <francois@schmidts.fr>"]
license = "GPLv3"
readme = "README.md"
packages = [{include = "prometheus_distributed_client"}]

[tool.poetry.dependencies]
python = "^3.7"
prometheus-client = "<0.12"
redis = ">=2.10.5"


[tool.poetry.group.dev.dependencies]
pytest = "^7.2.1"
mock = "^5.0.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
